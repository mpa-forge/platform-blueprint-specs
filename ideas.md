
code quality checks?
security checks?

staging: create stages on branches with frontend+backend  connected

why use a different repo for contracts? do we need to separate it from backend if we produce a client library for the frontend that will efectively constrain the usage of the api to the contracts?

ai automated dev:
- where is automation executed?
- hook or poll?
- how to manage task dependencies
- permission restriction to agents for pushing to master, so they can only do mrs