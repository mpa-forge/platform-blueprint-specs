name: ci

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: read
  pull-requests: read

jobs:
  ci:
    uses: mpa-forge/.github/.github/workflows/reusable-ci-go.yml@1bc325943d02d591979952caf6369eb43f54295c
    # Uncomment when this caller enables OIDC-backed publish in the shared workflow.
    # permissions:
    #   contents: read
    #   id-token: write
    #   pull-requests: read
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    with:
      working-directory: "."
      sonar-organization-key: ""
      sonar-project-key: ""
      image-build-enabled: false
      image-name: ""
      image-context: "."
      image-dockerfile: "Dockerfile"
      gar-location: ${{ vars.GCP_GAR_LOCATION }}
      gar-project-id: ${{ vars.GCP_PROJECT_ID }}
      gar-repository: ${{ vars.GCP_GAR_REPOSITORY_APPS }}
      gcp-workload-identity-provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
      gcp-service-account: ${{ vars.GCP_CI_SERVICE_ACCOUNT }}
